# ── Docker image uses THIS file (not requirements.txt)
# ── torch and torchvision are installed separately in the Dockerfile
# ── via the CPU-only wheel index to avoid downloading the 2GB CUDA build

fastapi==0.136.1
uvicorn==0.46.0
pillow==12.0.0
python-multipart==0.0.27   # REQUIRED for FastAPI UploadFile — was missing from original